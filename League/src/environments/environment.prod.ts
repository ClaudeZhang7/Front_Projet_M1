export const environment = {
    production: true,
    // correspond a l'url de notre api elle doit être caché !
    // l'url sera donné à travers les configs fait sur netlify.
    apiUrl: 'https://back-projet-m1.onrender.com'
  };
